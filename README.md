fchat-pidgin
------------

Pidgin plugin for using FList's chat. Forked from TestPanther's version over at https://code.google.com/p/flist-pidgin/

Cross Compilation
-----------------

Run `make prepare_cross` to fetch the dependancies you need to build a 32-bit Windows version of the plugin from Linux.
